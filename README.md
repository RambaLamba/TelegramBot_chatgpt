# 🤖 Телеграм-бот с интеграцией ChatGPT

Телеграм-бот, который взаимодействует с OpenAI ChatGPT API для генерации ответов на вопросы пользователей с сохранением контекста диалога.

## ✨ Функционал

*   **Интеграция с ChatGPT**: Генерация ответов на текстовые сообщения пользователей с использованием API OpenAI.
*   **Контекст диалога**: Бот сохраняет историю общения с каждым пользователем для формирования связанных ответов.
*   **Управление контекстом**:
    *   Команда `/start` — сбрасывает историю диалога и показывает приветственное сообщение.
    *   Кнопка **🆕 Новый запрос** — сбрасывает историю диалога и позволяет начать беседу заново.
*   **Логирование**: Все ключевые события (запуски, запросы, ошибки) логируются в консоль.
*   **Безопасность**: Конфиденциальные данные (токены API) хранятся в защищённом `.env` файле.

## 🚀 Полная инструкция по запуску

### 1. Подготовка репозитория
1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/RambaLamba/TelegramBot_chatgpt.git
    cd <папка-проекта>
    ```
2.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
    Если файла `requirements.txt` нет, установите библиотеки вручную:
    ```bash
    pip install pyTelegramBotAPI openai python-dotenv
    ```

### 2. Настройка конфигурации
1.  **Создайте файл `.env`** в корневой папке проекта (рядом с `main.py`).
2.  **Скопируйте содержимое из примера**:
    - Откройте файл `.env.example`.
    - Скопируйте его содержимое.
3.  **Заполните своими данными**:
    Откройте `.env` в текстовом редакторе и замените значения на ваши реальные ключи:
    ```ini
    TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather
    OPENAI_API_KEY=sk-ваш_ключ_от_OpenAI_API
    ```
    > **🔒 Безопасность:** Файл `.env` уже добавлен в `.gitignore`. **НИКОГДА** не публикуйте его и не делитесь ключами.

### 3. Запуск бота
1.  Убедитесь, что находитесь в папке проекта.
2.  Запустите бота:
    ```bash
    python main.py
    ```
    
3. Перейдите в Telegram и найдите своего бота по имени. 
4. Отправьте команду `/start` для начала работы.

## 📖 Использование

| Действие | Результат |
|----------|-----------|
| **Отправить `/start`** | Приветствие, сброс истории, появляется кнопка **🆕 Новый запрос** |
| **Написать текст** | Бот сгенерирует ответ с учётом истории диалога |
| **Нажать кнопку 🆕 Новый запрос** | История диалога очищается, можно начать новую тему |
| **Отправить `/help`** | Покажет справку по командам и кнопкам |

## 🛠 Технические детали

*   **Библиотеки**: `pyTelegramBotAPI`, `openai`, `python-dotenv`
*   **Хранение контекста**: История хранится в памяти в формате `{"role": "...", "content": "..."}`
*   **Логирование**: Только в консоль

## 📁 Структура проекта
TelegramBot_chatgpt/
├── main.py # Основной код бота
├── .env # Файл с ключами(заполнить перед запуском)
├── .env.example # Шаблон для .env (в репозитории)
├── requirements.txt # Зависимости Python

└── README.md # Эта инструкция

